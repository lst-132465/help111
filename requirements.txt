# ==============================================
# 传智杯智能面试助手 依赖清单（终极稳定版）
# 支持 Python 3.9 ~ 3.11（必须选3.11，兼容性最佳）
# 安装命令：pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ==============================================

# ------------------------------
# 核心框架
# ------------------------------
# Streamlit 1.35.0：经过验证的稳定版，无前端渲染bug
streamlit==1.35.0

# ------------------------------
# AI 大模型依赖
# ------------------------------
# 统一OpenAI兼容接口，支持智谱/讯飞/通义/豆包所有模型
langchain==0.1.17
langchain-openai==0.1.6

# （可选）如需单独使用智谱原生SDK，取消下面一行注释
# langchain-zhipuai==0.0.1

# ------------------------------
# 文件解析（简历/PDF/Word）
# ------------------------------
pandas==2.2.2          # 数据处理与Excel解析
python-docx==1.1.2     # Word文档解析
PyPDF2==3.0.1          # PDF文档解析

# ------------------------------
# 文本处理
# ------------------------------
jieba==0.42.1          # 中文分词（简历关键词提取）
scikit-learn==1.4.2    # 文本特征提取与相似度计算
numpy==1.26.4          # 科学计算基础库（所有库的通用依赖）

# ------------------------------
# 语音识别（本地离线转写）
# ------------------------------
# ✅ 修复：移除+cpu后缀（标准PyPI源不支持该格式，会导致云环境安装失败）
# 本地如需纯CPU版本，可手动改为 torch==2.2.2+cpu -f https://download.pytorch.org/whl/torch_stable.html
torch==2.2.2
openai-whisper==20231117  # 稳定版语音转写模型

# ------------------------------
# 通用工具
# ------------------------------
python-dotenv==1.0.1   # 环境变量管理（API密钥安全存储）

# ==============================================
# 部署说明：
# 1. 本地运行：在项目根目录执行 streamlit run app.py
# 2. Streamlit云部署：Python版本必须选择 3.11（torch 2.2.2不支持3.12+）
# 3. 如安装速度过慢，务必使用清华源（上面安装命令已包含）
# 4. 云环境会自动预装ffmpeg，无需额外配置
# ==============================================